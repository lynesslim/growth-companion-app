# Deepseek Implementation Plan - Phase 4

**QA REJECTION (PLEASE FIX):**
You successfully made the code changes, but you hallucinated the version number for `flutter_svg` in `pubspec.yaml` as `^22.0.0`, which does not exist! This broke `flutter pub get`.
Please update `pubspec.yaml` to use a real version like `^2.3.0` and run `flutter pub get` and `flutter analyze` to ensure the project compiles.


**QA REJECTION (PLEASE FIX):**
You exited successfully without actually modifying any files! Your internal log shows that you attempted to run a `grep` shell command which was auto-rejected due to permissions, so you gave up.
Please do NOT run shell commands with wildcards. Just use your internal file reading tools or read the specific files you need (`lib/src/features/home/widgets/social_drops_card.dart`, `lib/src/features/dashboard/dashboard_shell.dart`, etc.) and actually implement the SVG covers and Key Takeaways fixes.


## Goal
Implement Phase 4 of the feedback: Content Generation UI (Covers & Takeaways).

## Objectives
1. **Dynamic AI SVG Book Covers (Item #6):** Our backend Python script now successfully injects raw SVG strings directly into the `coverUrl` property of the `GrowthDrop` JSON. Update the Flutter UI (specifically `lib/src/features/books/book_flip_screen.dart` and `lib/src/features/home/widgets/growth_drop_card.dart`) to render these dynamic SVGs instead of the hardcoded placeholder assets.
2. **Key Takeaways UI Fix (Item #3):** The "Key Takeaways" in the dashboard are currently showing up as one giant block of text because the newlines aren't rendering properly. Update the UI widget responsible for displaying the summary/takeaways to ensure it parses bullet points or newlines correctly.

## Instructions & Ponytail Constraints
- **SVG Rendering:** Use `flutter_svg` to render the SVG strings. If `coverUrl` is an SVG string, you can use `SvgPicture.string()`. To ensure backwards compatibility with older items or errors, wrap it in a fallback that still shows the placeholder if parsing fails.
- You must add `flutter_svg` to `pubspec.yaml` if it is not already there.
- **Linebreaks:** For the Key Takeaways, you can either replace `\n` with actual widget spacing using a simple `String.split('\n')` loop, or just use a standard Text widget but ensure the string interpolation doesn't strip the newline characters. Keep it simple.
