Please implement the following exact changes:

### 1. Dashboard Navbar Opacity
- Modify `lib/src/features/dashboard/dashboard_shell.dart`.
- Change the background container's alpha value for the navbar from `0.65` to `0.50` (`AppColors.secondarySurface.withValues(alpha: 0.50)`).

### 2. "Thing To Do" Screen Updates
- Modify `lib/src/features/books/book_flip_screen.dart`.
- Find `_ActionableInsightsPage`.
- Change the title text from "Actionable Insights" (or its current title) to "Thing To Do".
- Convert the numbered list into a list of interactive Checkboxes.

### 3. Persistent Checkbox State
- Convert `_ActionableInsightsPage` to a `StatefulWidget`.
- Use `SharedPreferences` to load and save the boolean state of each checkbox.
- Use a unique storage key pattern like `drop_${widget.book.id}_insight_$index` to ensure the checked state is permanently tied to that specific drop and insight item.
