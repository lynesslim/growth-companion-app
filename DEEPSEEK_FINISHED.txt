Please implement the following logic fixes regarding the personal streak calculation:

### 1. Fix the `updateStreak` Logic in UserProvider
We will completely rewrite `updateStreak` inside `lib/src/providers/user_provider.dart` to strictly use calendar days, removing the broken 24-hour wall-clock rule.

**Logic:**
- Compare `DateTime.now()` (current calendar day) against the user's `lastDropDate` (converted to a calendar day).
- If `lastDropDate == today`, keep streak the same (already completed today).
- If `lastDropDate == yesterday`, increment streak by `+1`.
- If `lastDropDate < yesterday` (or null), reset streak to `1`.

- Rewrite `updateStreak()` to use pure calendar-day `DateTime(year, month, day)` comparisons.
- Remove the `now.difference(lastActive).inHours > 24` rule entirely.

### 2. Move the Streak Trigger to `markAsRead`
Currently, the streak and XP are awarded in `saveToJournal`. This will be moved so that simply finishing the book triggers the rewards.

- Inside `lib/src/providers/growth_drop_provider.dart`:
- Remove `await ref.read(userProvider.notifier).updateStreak();` and `updateXp(15)` from `saveToJournal`.
- Add them into `markAsRead(GrowthDrop currentDrop)`.
- **Validation:** Before awarding the streak/XP in `markAsRead`, we will strictly verify that `currentDrop` is *actually* today's drop:
  - Verify `currentDrop.date` matches today's calendar date (`DateTime(now.year, now.month, now.day)`).
  - Verify `currentDrop.focusArea != 'Social Drop'` (meaning it is a personal daily drop, not a friend's drop).
